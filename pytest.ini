[pytest]
testpaths = tests
addopts = -v --tb=short --ignore=tests/legacy
markers =
    unit: Pure unit tests with no external API dependencies
    integration: Tests requiring real API credentials or external services
    slow: Tests that take more than 30 seconds (model loading, network, etc.)
